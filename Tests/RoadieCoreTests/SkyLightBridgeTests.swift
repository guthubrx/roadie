import XCTest
@testable import RoadieCore

// SPEC-021 — Tests SkyLightBridge (T042)
// Note : skip automatique si headless (pas de display connecté)
final class SkyLightBridgeTests: XCTestCase {
    // Populated incrementally by T042
}
